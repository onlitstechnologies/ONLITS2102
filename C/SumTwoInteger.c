#include<stdio.h>	//preprocessor directive (include header file)
int main()			//entry point of the program (comments/remarks)
{
	int a, b, c;
	printf("Enter the first number: ");
	scanf("%d", &a);
	printf("Enter the second number: ");
	scanf("%d", &b);
	c = a + b;
	printf("The sum is %d.\n", c);
	return 0;
}

/*
	32 Keyword - dictionary/reserved word
	ANSI C	-	
*/
